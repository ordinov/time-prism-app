# CRITICAL: Development Rules

**DO NOT** build. EVER. The user handles all builds manually.

- Never run: `yarn dev`, `npm run dev`, `npm run build`, or similar commands
- Never run build commands with timeout
- If code changes require a rebuild, tell the user instead of doing it

